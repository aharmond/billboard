<h2 class="ui center aligned header">Which song do you want to add to the billboard?</h2>
<ul class="ui list">
<% @songs.each do |song| %>
  <div class="item">
  <%= link_to song.name, add_song_billboard_path(@board, song) %>
  </div>
<% end %>